# macOS 移行 申し送り（shift-scheduler-app 関連）

作成: 2026-06-05 / 対象: Windows → macOS 開発環境移行を担当する Claude Code セッション

このプロジェクト（`shift-scheduler-app` / Shifree）は本番運用中（Onedrop 塾、`https://shifree.com`）。移行で**未コミットの作業を失わないこと**が最優先。

---

## 1. 【最重要】移行で失ってはいけない未コミット変更

`C:\Users\tatsu\Github\shift-scheduler-app` の作業ツリーに、コミットしていない変更がある。**fresh clone（GitHub から新規クローン）すると消える**ので注意。

| ファイル | 状態 | 重要度 | 内容 |
|---|---|---|---|
| `CLAUDE.md` | Modified | **最重要** | OAuth 障害対応の handoff（RELAX 対策・再開ポインタ）。これを失うと再開地点が消える |
| `docs/notes/260602_onedrop-login-readonly.sql` | 新規 | 中 | 本番DB read-only 調査SQL |
| `docs/notes/260605_macos移行申し送り.md` | 新規 | 中 | 本ファイル |
| `docs/incident-2026-04-26-handoff.md` | Modified | 低 | OAuth と無関係（移行前から変更あり） |
| `docs/archive-from-shift-keisan-app/` | 未追跡 | 低 | OAuth と無関係 |
| `docs/business/` | 未追跡 | 低 | OAuth と無関係 |
| `.playwright-mcp/` | 未追跡 | 捨て可 | Playwright の作業生成物。移行不要 |

- HEAD = `3424cec`（PR #48）で、これは origin/main にも push 済み（コミット履歴自体は GitHub にある）。
- **推奨移行方式**: リポジトリフォルダを**丸ごとコピー**（未コミット込みで移送）。fresh clone する場合は、上表の Modified/新規ファイルを**別途コピーして上書き**すること。
- 代替案: 移行前に handoff をブランチに commit + push して GitHub 経由で持っていく（push は親セッションの判断待ち。現時点では未実施）。

## 2. macOS 側セットアップ（このプロジェクトを動かすのに必要）

- **git**: user.name=`tatsunoritojo` / user.email=`tatsunoritojo@gmail.com`
- **Vercel CLI**: macOS で `vercel login` の**再認証が必須**（auth トークンはマシンローカル。Windows のものは移らない）。`.vercel/project.json`（projectId/orgId）はリポジトリ同梱なので link 自体は残る。team=`tatsunoris-projects-e56d1422` / project=`shifree`。
- **Python**: 3.9+。`python3 -m venv` → `pip install -r requirements.txt` → `pytest`（387件想定）。標準コマンドは `CLAUDE.md`「標準コマンド」参照。
- **機密ファイル**: `.env.local`（GOOGLE_CLIENT_SECRET / SECRET_KEY / DATABASE_URL 等）は**中身を読まずに安全に移送**し、リポジトリには載せない（`.gitignore` 済）。本番 env は Vercel ダッシュボード管理なのでローカルには最小限でよい。

## 3. サーバー側設定（移行で触る必要なし・情報共有）

- **`OAUTHLIB_RELAX_TOKEN_SCOPE=1` は Vercel の Production env（サーバー側）にある**。ローカルファイルには存在しない。macOS 移行で再設定不要。消さないこと。
- 本番は Vercel 上で稼働継続中。移行作業は本番に影響しない（ローカル開発環境の引っ越しのみ）。

## 4. 中断した作業の再開ポイント（OAuth verification 対応）

- **現在地**: PR #48 後の legacy scope mismatch ログイン障害を、`OAUTHLIB_RELAX_TOKEN_SCOPE=1`（Production env + redeploy `dpl_GUxyPYNPerAW9tKpU5o4rUVxt6Q2`）で解消・実証済み。影響14アカウント、revoke 不要で全員救済。
- **判定6項目: 5/6 完了**。残りは **#5 Calendar 書き込みテスト（`events().insert()`）**のみ。
  - 読み取り（calendars / events GET）は 200 で実証済み。
  - 「シフト提出」は内部DB書き込みで calendar.events を使わない（テスト対象外）。
  - 次にやること: owner（tatsunoritojo id=5）でシフト承認 → 確定 → Google カレンダー同期 → `POST /api/worker/confirmed-shifts/<id>/sync` の 2xx と calendar エラー不在を Vercel ログ（deployment `dpl_GUxyPYNPerAW9tKpU5o4rUVxt6Q2`）で確認。
- **#5 合格後**: GCC Data Access から `calendar.events.readonly` を削除 → OAuth verification submission。
- **詳細の正典**: `CLAUDE.md`「OAuth Verification 本番公開対応」セクション（特に「legacy scope 救済（OAUTHLIB_RELAX_TOKEN_SCOPE）」と「次セッション着手用ポインタ」）。

## 5. 移行完了後の最初の一手（OAuth 再開時）

1. リポジトリの未コミット変更が移送できているか確認（`git status` で上表と一致するか）
2. `CLAUDE.md` の OAuth セクションを読む
3. Calendar 書き込みテスト（#5）から再開
